/**
 * Single config file — edit these values after setup.
 * See README.md for Google Drive, GitHub Pages, and password hash steps.
 */
window.SITE_CONFIG = {
  /** GitHub repo as "username/repo-name" (shown in footer) */
  githubRepo: "gabi-in-mtl/gabi-in-mtl",

  /** Folder ID from: https://drive.google.com/drive/folders/THIS_PART */
  driveFolderId: "YOUR_GOOGLE_DRIVE_FOLDER_ID",

  /** Google Cloud API key (Drive API, HTTP referrer restricted) */
  googleApiKey: "YOUR_GOOGLE_API_KEY",

  /** SHA-256 hex of your site password — never put the plain password here */
  passwordHash: "YOUR_PASSWORD_SHA256_HEX",
};
